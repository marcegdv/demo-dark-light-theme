import { style } from "typestyle";

const styles = {
    container: style({
        padding: '8px',
        // overflowWrap: 'normal',
        // wordBreak: 'break-word',
        // wordWrap: 'break-word',
    })
};

export default styles;